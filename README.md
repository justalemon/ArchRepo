# Lemon's Arch Repo

This is my own private repo that contains the packages I need that are not available on the official repo. You can see the packages listed on packages.yml.


## Building the packages

Run `python compiler.py --build`. You can use `--package` to build specific packages you need.

## Building the repository

TODO
