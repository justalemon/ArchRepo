services:
  nginx:
    image: nginx:1.31.4
    restart: unless-stopped
    ports:
      - "8047:80"
    volumes:
      - ./repo:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
